# 2026世界杯预测项目 - 项目规则

> v12: 全文重组织 — 每个主题只出现一次，不再零散追加

---

## ⏱️ 一、时间与日期

- **所有时间统一使用北京时间 (UTC+8)**，标注为"北京时间"
- 文档生成时间也标注北京时间

### ESPN API 时区（🚨 每次拉数据前必读）

> ESPN API 返回的 `date` 字段是 UTC 时间（如 `2026-06-24T19:00Z`），**不是北京时间**。
> `scoreboard?dates=20260624` 返回的是美国当地日期，与北京有8小时时差。
> **禁止用 `dates=` 参数按北京日期筛选。**

**每次拉取赛程/比分的标准代码：**

```python
import urllib.request, json
from datetime import datetime, timedelta

url = 'https://site.api.espn.com/apis/site/v2/sports/soccer/fifa.world/scoreboard'
req = urllib.request.Request(url, headers={'User-Agent': 'Mozilla/5.0'})
j = json.loads(urllib.request.urlopen(req, timeout=15).read())

for evt in j.get('events', []):
    utc_str = evt.get('date', '')           # "2026-06-24T19:00Z"
    utc_dt = datetime.fromisoformat(utc_str.replace('Z', '+00:00'))
    bjt_dt = utc_dt + timedelta(hours=8)    # 北京时间
    bjt_date = bjt_dt.strftime('%Y-%m-%d')  # 如 "2026-06-25"
    bjt_time = bjt_dt.strftime('%H:%M')     # 如 "03:00"
    # 按 bjt_date 分组，不是 ESPN 返回的日期
```

| 字段 | 含义 | 示例 |
|------|------|------|
| `evt['date']` | UTC 时间 | `2026-06-24T19:00Z` |
| 换算后 | 北京时间 | `2026-06-25 03:00` |

- `status.type.state`: `'pre'`=未开始, `'in'`=进行中, `'post'`=已结束

---

## 📐 二、命名规范

### 球员名
- **所有球员名必须使用中文译名**，禁止英文名
- 不常见球员首次出现时括号标注原文: `法伊祖拉耶夫(Fayzullaev)`
- 唯一允许的简称: C罗、B费（已是通用称呼）

### 国家名
- **必须使用全称**，全文禁止任何单字缩写
- ❌ 禁止: 哥、苏、葡、英、克、巴、摩、加、日、韩、刚 ……
- ✅ 正确: 哥伦比亚、苏格兰、葡萄牙、英格兰、克罗地亚、巴西、巴拿马、摩洛哥、加拿大、加纳、日本、韩国、刚果民主共和国(刚果金)
- ⚠️ 批量替换时注意: "摩洛**哥**""墨西**哥**""乌兹别**克**""捷**克**"等词内部含禁字

### 比分格式
- `阿根廷 2-0 奥地利` | 半场: `上半场 1-0`
- 进球者: `梅西 38' 90+'` (中文名 + 分钟)

### 文件命名
- `.md`: `YYYY年M月D日_主题.md`
- `.txt`: `YYYY年M月D日_主题_手机版.txt`

---

## 📤 三、输出规范 — 每次生成 .md + .txt

### .md 排版
1. 标题: `##` 主标题 → `###` 比赛标题 → `####` 小节
2. 表格列宽: 每列 ≥ 最长内容 + 2空格。球员名列给到7字宽。生成后目测，挤到换行就加宽
3. 重点用 `**粗体**`

### .txt 排版 — 🚨 内容必须与 .md 完全一致

> 只改排版，不删内容。.md 有多少模块，.txt 就有多少模块。

```
行宽 ≤40半角 (~20中文) | 分隔符: ===== ------ ━━━━━
表格 → 字段:值 格式 | 序号用 [1] [2] | 重点用 * -> >
禁止: emoji国旗, box-drawing, ≥3列表格, markdown链接
底部附参数卡
```

**生成后自检:** Read 两份文件 → 对比模块数 → 缺失立刻补

---

## 📋 四、预测/复盘 必填清单

> 唯一清单。每次预测必须全部覆盖。

| # | 项目 | 格式要求 |
|---|------|---------|
| 1 | **首轮球员评分** | 中文名 + 评分 + 表现 + 标注 MOTM 和最低分 |
| 2 | **因素导向表** | `因素 \| 对哪边有利 \| 理由` — 每行必须标方向 |
| 3 | **冷门风险评估** | 低/中/高/极高 + 理由 |
| 4 | **比分预测** | 首选 + 半场 + ≥2个备选 |
| 5 | **伤病/停赛** | 含"已验证可用替补" |
| 6 | **强队分类** | 超级巨星型 / 体系型 / 低效热门 |
| 7 | **亚非韧性评估** | 5项: 低位防守/速度反击/定位球高点/前30分钟/被压制不崩盘 |
| 8 | **教练博弈** | 落后/领先策略 + 替补改变比赛能力 |
| 9 | **定位球攻防** | 简评 |
| 10 | **冷门路径** | 如果翻车，最可能怎么翻 |

---

## 🔬 五、分析铁律

### 1. 亚非球队不低估
面对强队时，亚洲/非洲球队防守纪律往往更强。不做5项韧性评估 = 不合格。
→ 案例: 英格兰0-0加纳，加纳5/5韧性满分

### 2. 强队三类分法

> 三类分法的目的是预测强队面对大巴时的破局能力。

| 类型 | 定义 | 破大巴能力 | 预测倾向 |
|------|------|-----------|---------|
| **超级巨星型** | 有≥2名具备"单兵破局"能力的球员，且均有本赛季顶级联赛验证 | 强 — 个人能力可强行破局 | 可高看大胜 |
| **体系型** | 进攻依赖整体配合，核心球员一旦被锁即无第二方案 | 弱 — 大巴是最致命的克制 | 防小胜/防平局 |
| **低效热门** | 纸面强但近期状态差、更衣室乱、或历史数据不支持 | 取决于具体问题 | 防冷平 |

**判定标准（用数据，不凭感觉）:**
- 超级巨星 = 本赛季五大联赛进球/助攻均 ≥10，或身价 ≥8000万欧，且有欧冠淘汰赛证明
- 如果只有1名超级巨星 → 仍是体系型（对方只需锁死一个人）
- ≥2名超级巨星 + 不同进攻方式（如边锋+中场+中锋）→ 超级巨星型
- 示例: 葡萄牙(C罗+B费+莱昂) = 超级巨星型 → 5-0。英格兰(凯恩1人+贝林厄姆不稳定) = 体系型 → 0-0。巴西(维尼修斯+库尼亚+内马尔) = 超级巨星型。

### 3. 禁止绝对化
禁用"必胜""稳赢""一定打穿""毫无疑问""不可能输"
→ 替代: "更倾向于""大概率""首选方向"
每个结论附: 主要不确定性 + 潜在冷门路径

### 4. 换人必须双面评估
边翼卫/后卫换下时，同时评估两问:
- ① 进攻贡献(传中/定位球/反击)是否消失？
- ② 替补者的防守是否不如被换下者？
→ 案例: 6/24马苏阿库被换下，分析只说①(定位球移除利好哥伦比亚)，漏了②(左路防守减弱→穆尼奥斯76'右路破门)。且分析写利好，预测却写0-0 = 自相矛盾。

### 5. 比赛独立分析
禁止用一场比赛的结果推导同天另一场。不存在"X为全天定调"。
→ 案例: 6/24英格兰0-0加纳 ≠ 哥伦比亚也会0-0

### 6. 教训必须数据先行
先跑数据验证假设，再写教训。不要先写教训再找数据。
无可靠数据支撑的"教训" → 写成"待验证假设"。

---

## 📊 六、历史基线

| 比分 | 占比 | 含义 |
|------|------|------|
| 1-1 | 11.3% | 最常见比分 |
| 1-0 | 10.8% | 一球小胜 |
| 0-0 | 9.4% | 近1/10 |
| 2-0 | 7.3% | 比你想象的低 |

| 轮次 | 场均进球 | 0-0率 | 特征 |
|------|---------|--------|------|
| 第1轮 | 2.63 | 9.3% | 试探 |
| 第2轮 | 2.81 | 10.4% | 高0-0 |
| 第3轮 | 2.63 | 8.1% | 出线明朗 |

---

## 🛡️ 七、防幻觉自查

### 复盘前（硬性第一步 — 不准跳过）

```
[0] Glob 搜索 + Read 原始预测文件 → 把预测数据复制到复盘文档后，才开始写
[1] ESPN API 确认当天实际比赛+比分（UTC+8换算北京日期）
[2] AnySearch 交叉验证比分+球员评分
[3] 逐场对比: 表格写的预测 = [0]读到的原始预测
[4] 复查: 有没有比赛写了"无正式预测"但实际有预测?
```

**特别禁止:**
- 凭记忆写预测 → 必须对照Read的原始文件逐字复制
- 写"无正式预测" → 必须先Glob确认真的没有
- 觉得"大概是这样" → 不准"大概"

### 预测前

```
[1] ESPN API 确认当天赛程（UTC+8换算北京日期）
[2] AnySearch 获取小组形势/伤病/场外新闻
[3] 每场比赛必须出现在[1]的返回中
```

### 赛中/半场

```
[0] 比分确认 ≠ 大名单确认
[1] AnySearch 确认26人大名单+本场替补
[2] "建议换XX" → XX必须在替补名单中
[3] "XX缺阵" → 必须有搜索结果支撑
[4] 预测文档必须包含"已验证可用替补"
```

---

## 🔧 八、数据工具

### AnySearch（主力搜索）
```bash
python ~/.claude/skills/anysearch/scripts/anysearch_cli.py search "query" --max_results 5
python ~/.claude/skills/anysearch/scripts/anysearch_cli.py extract "URL"
```

### ESPN API（实时数据）

> 标准代码见 **一、时间与日期** 章节。核心: 拉全量 → UTC+8 → 重新分组。

### openfootball JSON（赛后验证）

| 日期范围 | 可靠性 |
|---------|--------|
| 1930-2022 | ✅ 可靠 |
| 2026 6/11-6/22 | ✅ 可靠 (14/14 验证通过) |
| 2026 6/23 | ⚠️ 需交叉验证ESPN (1个已知偏差) |
| 2026 6/24+ | ❌ 0-0占位符，不可用 |

```bash
python openfootball_data.py 2026 --standings
python openfootball_data.py 2026 --review "file"
```

